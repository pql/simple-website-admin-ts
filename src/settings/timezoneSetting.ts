import { TimezoneSetting } from '#/config';

export const timezoneSetting: TimezoneSetting = {
  showPicker: true,
  // timezone
  offset: '+08:00', // 默认东八时区
  zone: 'Asia/Shanghai',
  names: [],
};
