import { FireBytes_Unified_ButtonGroups_Dtos_ButtonConfigs } from '@/apis';

export const pageToolbarButtonProps = {
  config: {
    type: Object as PropType<FireBytes_Unified_ButtonGroups_Dtos_ButtonConfigs>,
    required: true,
  },
};
